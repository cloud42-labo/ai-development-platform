# Schemas

Structured, machine-interpretable definitions. See `../docs/versioning-policy.md`
for what MAJOR/MINOR/PATCH mean for this class (`schema_version` in
`../adp-package.yaml`).

| File | What it defines |
|---|---|
| [`../governance/agent-policy.yaml`](../governance/agent-policy.yaml) | Agent action authorization policy draft — **not yet an approved, enforced control** (see the Freeze-scope exception in `../docs/v1-asset-inventory.md` and `../adp-package.yaml`'s `schema_version: 0.1.0-experimental`) |

No other Schema-class files exist in this repository today (`../docs/v1-asset-inventory.md`).
